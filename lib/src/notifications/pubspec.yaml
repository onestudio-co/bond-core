name: bond_notifications
description: notifications is a Bond package provides a convenient way to handle notifications.
version: 0.0.1
homepage: https://github.com/onestudio-co/flutter-bond
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

environment:
  sdk: ">=2.16.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  bond_core:
    path: ../core

  bond_cache:
    path: ../cache

  bond_network:
    path: ../network

